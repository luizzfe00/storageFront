import { base64toBlobURL, dataURLtoFile } from './base64Converter';
import { inputFormatter } from './date';
import * as dateFormatting from './dateFormatting';
import { formatCurrency, removeFormatting } from './formatCurrency';

const UtilsFunctions = {
  base64toBlobURL,
  dataURLtoFile,
  inputFormatter,
  formatCurrency,
  removeFormatting,
  dateFormatting,
};

export default UtilsFunctions;
